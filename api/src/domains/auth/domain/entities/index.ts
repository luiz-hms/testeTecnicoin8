/**
 * Auth Domain Entities
 *
 * Currently, authentication entities (User) are defined in the user domain
 * to maintain a clear separation of concerns. This folder can be extended
 * with auth-specific entities if needed in the future.
 *
 * @see @user/domain/entities for User entity
 */
