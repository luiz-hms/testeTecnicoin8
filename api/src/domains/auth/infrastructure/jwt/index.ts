/**
 * JWT Infrastructure Configuration
 *
 * JWT configuration and strategies are handled through:
 * - @auth/infrastructure/strategies/jwt.strategy.ts - JWT validation logic
 * - @auth/auth.module.ts - JWT module registration with secret management
 *
 * This folder is reserved for future JWT-specific utilities if needed
 * (e.g., JWT token decorators, custom JWT error handlers, etc.).
 *
 * @see @auth/infrastructure/strategies for JWT strategy
 * @see @auth/auth.module for JWT module configuration
 */
